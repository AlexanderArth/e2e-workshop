describe("Create person page", () => {
  it("sends the correct request to the backend", () => {
    /*
     * given: prefilled create user form
     * when: create button is clicked
     * then: a request is sent to the backend with the correct information
     */
  });
});

describe("Person list page", () => {
  it("displays the users listed in the backend response", () => {
    /*
     * given: user is logged in
     * when: the list page is visited
     * then: the result of the backend request is displayed
     */
  });
});
