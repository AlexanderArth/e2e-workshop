describe("Landing Page", () => {
  it("switches from light to dark mode", () => {
    /*
     * given: User is logged in with light mode
     * when: User toggles dark mode in header menu
     * then: Dark mode toggles on
     */
  });
});
