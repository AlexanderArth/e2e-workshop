/*
 * Perform some tests with a lower resolution (e.g. iPhone 6).
 * See https://docs.cypress.io/api/commands/viewport.html#Syntax for help.
 * Use cy.screenshot() for reporting different states.
 * Observe the auto scrolling to elements which really comes in handy here.
 *
 * For example replicate the person creation test and interact with the list
 */
